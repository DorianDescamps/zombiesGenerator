networks:
  proxy-net:
    external: true

services:
  zombies:
    networks:
      - default
      - proxy-net
    environment:
      - "VIRTUAL_HOST=zombies.<domain>"
      - "VIRTUAL_PORT=8080"
      - "LETSENCRYPT_HOST=zombies.<domain>"
